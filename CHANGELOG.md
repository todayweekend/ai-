# 变更记录

本文件记录这个项目的对外变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-19

首个公开版本。

### 核心

- **多成员**：想加几个 AI 就加几个。每个成员可以走任意 **OpenAI 兼容接口**
  （DeepSeek / Kimi / GLM / 通义 / 豆包 / 本地 Ollama…），各带自己的 Key、
  模型、人设文件和工作区。
- **单聊 + 群聊**：群聊里多个 AI 同场混着说 —— 点名必回、没点名各自判断要不要
  接话、可设「组长 / 平权 / 旁听」；干完一步能自动接力给下一位，总轮数有上限，
  不会无限套话。
- **12 个工具**：读文件 / 精准编辑 / 移动改名 / 读指定行 / 写文件 / 列目录 /
  递归搜索工作区 / 跑命令 / 检索与追加共享记忆 / 联网搜索 / 抓网页正文。
- **两种工作引擎**：内置的接口引擎，或把某个成员换成「外部命令引擎」——
  把话交给一条本机命令，取它的 stdout 当回复。
- **共享记忆 + 命名铁律**：所有 AI 共用一个目录，每人只能写自己前缀的文件
  （代码层强制），多个 AI 交替干活不会互相覆盖。
- **插件系统**：11 个钩子 + 前端 `window.WB` 接口 —— 界面（背景 / 配色 /
  按钮 / 布局）、人格、模型、回复文案、自定义 `/指令`、自定义接口与实时推送，
  全都不用动核心代码。
- **MCP 接入**：填一行启动命令，社区现成的 MCP 工具立刻变成 AI 可调用的工具。
- **过程可见**：工具卡片带耗时；一轮结束给 token 用量汇总（provider 不返回
  用量时按字符估算并标注）；右侧「产出文件」抽屉列出这轮写出的所有文件。

### 工程

- **零构建、零前端框架**：克隆下来配个 Key 就能跑，没有打包步骤。
- 启动自检 `check.py` 会逐个成员实测接口，Key 失效只记警告、不阻止启动。
- 附带两个端到端测试（插件系统 / 工作引擎），CI 自动跑。

### 安全默认值

- 默认**只监听 `127.0.0.1`**，仅本机可访问；要开放给局域网需要显式用
  `WORKBENCH_HOST=0.0.0.0` 打开。原因见 [SECURITY.md](SECURITY.md) ——
  这个工作台的 `run_command` 会在宿主机上真执行命令。
