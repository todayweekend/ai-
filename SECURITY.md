# 安全说明

多 AI 工作台是一个**在自己电脑上运行**的本地工具。为了让 AI 能真正干活，它有意
提供了几个「很能干」的能力。**请先读完这一页再决定给谁用。**

## 这个项目能做什么

| 能力 | 意味着什么 |
|---|---|
| `run_command` 工具 | 模型会让它在你电脑上**真执行 shell 命令** —— 跟你自己开个终端敲一遍是同等的 |
| 外部命令引擎 | 成员可以配置成「把话交给一条本机命令」，那条命令会被**直接执行，没有二次确认** |
| 插件系统 | 插件跑在服务进程内，**等同于本机代码权限**（能读文件、发网络请求、改界面） |
| MCP 接入 | 接进来的 MCP 服务也是本机进程，权限同样等同于本机 |
| 工作区读写 | 每个成员能在自己的 `workspace` 目录里读写文件；越界路径会被拒绝 |

**一句话结论：只运行你自己信得过的模型、命令和插件。** 这不是沙箱，也没有被设计成
沙箱。给它多大权限，取决于你把什么交给它。

## 默认的网络暴露

发行版**默认只监听 `127.0.0.1`**，也就是只有本机能访问。

如果你按 README 里的说明把它改成 `0.0.0.0`，那么**同一网络里的任何人都能打开
这个页面** —— 而页面背后是可以驱动 AI 执行命令的。**要放到公网，必须自己加上
认证与反向代理**，不要把裸服务直接暴露在互联网上。

## 已知的设计取舍（这些不算漏洞）

- **API Key 明文存在本地**：`config.json` 里的 key 是明文（文件权限取决于你的系统）。
  它不会被下发到前端，接口返回 key 时只给打码串。
- **聊天记录 / 共享记忆 / 工作区文件都是本地明文**：它们就是普通文件。
- **前端不做额外隔离**：它假设使用者就是本机用户本人。
- **模型的输出不做内容审查**：模型说什么、做什么，取决于你接的是哪个模型。

## 上报安全问题

如果发现的是**真漏洞**，例如：

- 能读写成员工作区之外的路径；
- 绕过防撞锁 / 成员信任机制去改动程序代码；
- 让未授权的远端访问者执行命令或读到 key；

请**不要开公开 issue**，改用本仓库的

> **Security → Report a vulnerability**

私下提交，附上复现步骤和影响范围。

如果只是「我不确定这算不算问题」，开 issue 讨论就好，没有关系。
